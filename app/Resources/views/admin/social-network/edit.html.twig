{% extends "admin/layout.html.twig" %}

{% block breadcrumb %}
    <div class="row wrapper border-bottom white-bg page-heading">
        <div class="col-lg-10">
            <h2>{{ socialNetwork.socialNetwork.label }}</h2>
            <ol class="breadcrumb">
                <li>
                    <a href="{{ path('admin_dashboard') }}">Dashboard</a>
                </li>
                <li>
                    <a href="{{ path('admin_social_networks') }}">Réseaux sociaux</a>
                </li>
                <li class="active">
                    <strong>{{ socialNetwork.socialNetwork.label }}</strong>
                </li>
            </ol>
        </div>
    </div>
{% endblock %}

{% block wrapper_content %}
    <div class="row wrapper wrapper-content animated fadeInRight">
        <div class="col-lg-12">
            <div class="ibox">
                <div class="ibox-content">
                    {{ form_start(form, {'attr': {'class': 'form-horizontal'} })  }}
                    <div class="form-group">
                        {{ form_label(form.url, null, {'label_attr' : {'class' : 'col-lg-2 control-label'} }) }}
                        <div class="col-lg-10">
                            {{ form_widget(form.url, {'attr' : {'class' : 'form-control'} }) }}
                            {{ form_errors(form.url, {'attr' : {'class' : 'text-danger'} }) }}
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_label(form.socialNetwork, null, {'label_attr' : {'class' : 'col-lg-2 control-label'} }) }}
                        <div class="col-lg-10">
                            {{ form_widget(form.socialNetwork, {'attr' : {'class' : 'form-control'} }) }}
                            {{ form_errors(form.socialNetwork, {'attr' : {'class' : 'text-danger'} }) }}
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-lg-2"></div>
                        <div class="col-lg-10">
                            <button class="btn btn-sm btn-info" type="submit">Enregistrer</button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
